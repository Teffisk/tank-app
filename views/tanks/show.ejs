<div class="container">
	<h1><%= tank.short_name %></h1>

	<img src="<%= tank.images %>">
	<ul>
		<% packages.forEach(function(p) { %>
			<li>
				<%= p.engine.name %>
			</li>
		<% }) %>
	</ul>
</div>